# HTML input

Treat HTML as behavioral and structural evidence, not as the visual standard. Identify regions, repeated items, forms, and actions, but do not recreate narrow-screen or responsive-state pages. Replace arbitrary CSS with Kit tokens and Ant Design components at the fixed `1920` desktop width. Preserve meaningful content and interaction semantics; discard implementation artifacts and accidental styling.
