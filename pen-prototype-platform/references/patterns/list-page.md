# List page

Use a clear title and primary action, followed by search/filter controls, result feedback, and a table or list. Keep row actions predictable; move rare actions into an overflow menu. Include loading, empty, no-result, error, and pagination states where relevant. Filters must visibly affect the result set and remain recoverable.
