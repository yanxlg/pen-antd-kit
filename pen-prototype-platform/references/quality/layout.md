# Layout quality

Use the Kit spacing and typography scale. Maintain a consistent content width, grid, vertical rhythm, and action alignment. Avoid nested cards without semantic need. Test representative long labels, dense rows, error messages, and smaller viewports so the composition does not depend on ideal content.
