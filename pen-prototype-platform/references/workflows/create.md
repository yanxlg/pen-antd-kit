# Create workflow

Turn the source into a page inventory and primary task flow. Choose one page pattern per page, query matching templates, and establish the shell before filling sections. Build in dependency order: navigation, primary content, actions, secondary content, states, annotations. Use library instances from the beginning so later replacement is unnecessary.
