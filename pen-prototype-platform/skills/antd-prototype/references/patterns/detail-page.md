# Detail page

Lead with identity, status, and primary actions. Group attributes by user task rather than database schema. Use tabs only for genuinely distinct subdomains. Represent loading, unavailable sections, permission restrictions, and destructive confirmations where required.
