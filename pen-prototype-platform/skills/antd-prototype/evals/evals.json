{
  "skill": "antd-prototype",
  "cases": [
    {
      "id": "markdown-list-page",
      "prompt": "根据 requirements.md 设计一个用户与权限管理 Pen 原型",
      "expected": [
        "resolves markdown input and list-page guidance",
        "runs library preflight before node creation",
        "queries rather than loading the full registry",
        "includes loading, empty, error, and permission states"
      ]
    },
    {
      "id": "html-form-normalization",
      "prompt": "把 existing-form.html 转成标准 Ant Design Pen 原型",
      "expected": [
        "uses HTML as structural evidence instead of preserving arbitrary CSS",
        "checks for a form template without forcing a mismatch",
        "uses verified library components",
        "models validation and submission states"
      ]
    },
    {
      "id": "pen-revision",
      "prompt": "优化 rough.pen，保留已有流程并统一为当前组件库",
      "expected": [
        "routes to revise workflow",
        "does not rebuild unaffected regions",
        "verifies library provenance",
        "performs visual review"
      ]
    }
  ]
}
