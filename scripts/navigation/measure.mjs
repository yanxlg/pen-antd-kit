import {chromium} from '@playwright/test';import {setup} from '../floatbutton-browser.mjs';
const browser=await chromium.launch({channel:'chrome'});const page=await browser.newPage();await setup(page);await page.addStyleTag({content:'*{transition:none!important;animation:none!important}'});
for(const props of [{},{size:'small'},{orientation:'vertical'},{type:'navigation'},{type:'panel'},{type:'dot'},{type:'inline'}]){
const result=await page.evaluate(async props=>{ReactDOM.render(React.createElement(antd.ConfigProvider,{theme:{token:{fontFamily:'Inter'}}},React.createElement(antd.Steps,{current:1,style:{width:740},items:[{title:'Finished',content:'This is a content.'},{title:'In Progress',content:'This is a content.',subTitle:'Left 00:00:08'},{title:'Waiting',content:'This is a content.'}],...props})),document.getElementById('root'));await new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r)));const root=document.querySelector('.ant-steps'),r=root.getBoundingClientRect();return [...root.querySelectorAll('[class^="ant-steps-item"]')].map(n=>{let b=n.getBoundingClientRect();return {c:n.className,x:+(b.x-r.x).toFixed(1),y:+(b.y-r.y).toFixed(1),w:+b.width.toFixed(1),h:+b.height.toFixed(1)}});},props);console.log(JSON.stringify({props,result}));}
await browser.close();
